// Package models contains shared domain types for ThreatLens.
// Core types (Target, CollectedData, ScanResult, Finding, Fingerprint, etc.)
// are defined in target.go, result.go, finding.go, and report.go.
package models
